module.exports.cors = {
  enabled: false, // Change to true to enable CORS support
  allowedDomains: '*', // pass in a single domain or an array of domains
  allowedMethods: ['GET', 'POST', 'PUT', 'DELETE'] // pass in a single method or array of methods
}
