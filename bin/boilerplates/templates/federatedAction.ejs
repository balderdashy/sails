<%
/**
 * Boilerplate federated controller
 * 
 * This is the template used when creating federated controllers 
 * generated through the CLI
 * e.g. `sails generate controller foo action -f`
 *
 */
%>/**
 * /<%= entity.toLowerCase() %>/<%= action %>
 */ 

module.exports = function () <%= action %> (req,res) {
  
  // This will render the view: 
  // <%- viewPath %>/<%-entity%>/<%- action %>.<%- viewEngine %>
  res.view();

};
