<%
/**
 * Boilerplate action
 * 
 * This is the template used when creating actions for controllers 
 * generated through the CLI
 * e.g. `sails generate controller foo action`
 *
 */
%>
  /**
   * If action blueprints are enabled, automatically reachable from:
   * /<%= entity.toLowerCase() %>/<%= action %>
   */ 
  <%- action %>: function (req, res) {

    /*
     * NOTE: This function is Sails middleware-- that means that not only do `req` and `res`
     * work just like their Express equivalents for HTTP requests, they also simulate
     * the same interface for receiving socket messages.
     */

    // Send JSON:
    // return res.json({ foo: 'some data the client needs' });

    // -OR-

    // Render a view with some locals: 
    // (<%- viewPath %>/<%-entity%>/<%- action %>.<%- viewEngine %>)
    // return res.view({ foo: 'some data for the view' });

    // -OR-

    // Redirect to another route:
    // return res.redirect('/foo/bar')

  }