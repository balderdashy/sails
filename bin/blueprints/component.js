/*---------------------
	:: <%- name %> 
	-> component
---------------------*/
Mast.registerComponent('<%- name %>', {

	// Use extendsFrom to inherit methods and events from another component
	// extendsFrom: "nameOfSomeOtherComponent",

	// Use events to listen for DOM events (like Backbone)
	// events: {
	//	click: 'displayAlertMessage'
	// },

	// Define methods to contain UI logic (keep in mind: it's best not to make these methods event-specific)
	// displayAlertMessage: function (e) {
	//	alert("You've clicked on me!");
	// },

	// Use subscriptions to listen to broadcasted events from the server-side
	// subscriptions: {}
	
});